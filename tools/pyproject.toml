[project]
name = "wiki-tools"
version = "0.1.0"
description = "WIKI 语料转换工具（PDF → Markdown）"
requires-python = ">=3.12"
dependencies = [
    "pdfplumber>=0.11",
    "pymupdf>=1.24",
]

[tool.uv]
package = false
